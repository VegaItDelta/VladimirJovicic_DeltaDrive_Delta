import { ReviewModule } from '../../../src/model';

describe('ReviewModule', () => {
  const module = new ReviewModule();

  it('should define the module', () => {
    expect(module).toBeDefined();
  })
});