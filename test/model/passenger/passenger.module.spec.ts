import { PassengersModule } from '../../../src/model';

describe('PassengerModule', () => {

  const module = new PassengersModule();

  it('should create', () => {
    expect(module).toBeDefined();
  })
});